import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.tranhaidang.tiniquiz",
  appName: "Tiniquiz",
  webDir: "dist",
  ios: {
    backgroundColor: '#0f1419'
  }
};

export default config;
